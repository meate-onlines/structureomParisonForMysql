# 数据库表结构比较工具依赖包

# MySQL数据库连接器
mysql-connector-python==8.2.0

# PostgreSQL数据库连接器
# 在Windows上使用预编译的psycopg2-binary，在Linux/Mac上可以使用psycopg2
psycopg2-binary>=2.9.9; sys_platform == "win32"
psycopg2>=2.9.9; sys_platform != "win32"

# SQLite是Python内置模块，无需额外安装

# SSH隧道支持
sshtunnel>=0.4.0

# 其他可能用到的包
typing-extensions==4.8.0